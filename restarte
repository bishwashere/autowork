if [ $1 = 'all' ]
then
	history -a
	
	sudo supervisorctl restart all
elif [ $1 = 'celery' ]
then
	sudo supervisorctl restart celery
elif [$1 = 'gunicorn' ]
then
	sudo supervisorctl restart gunicorn
elif [ $1 = 'crashmail' ]
then
	sudo supervisorctl restart crashmail
fi

